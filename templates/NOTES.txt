1: jwt-secret secret ını oluştur
kubectl get secret jwt-secret -o yaml

3: sendgrid secret ını oluştur
kubectl  get secret sendgrid -o yaml

4: vault secret ını oluştur
kubectl get secret vault -o yaml



1: xebula-rsa secret ını oluştur
kubectl get secret xebula-rsa -o yaml

2: keycloak secret ını oluştur
kubectl get secret keycloak -o yaml